Link: [485. Max Consecutive Ones](https://leetcode.com/problems/max-consecutive-ones/description/) <br>
Tag : **Easy**<br>
Lock: **Normal**

Given a binary array `nums`, return *the maximum number of consecutive *`1`*'s in the array*.

**Example 1:**
```
Input: nums = [1,1,0,1,1,1]
Output: 3
Explanation: The first two digits or the last three digits are consecutive 1s. The maximum number of consecutive 1s is 3.
```
**Example 2:**
```
Input: nums = [1,0,1,1,0,1]
Output: 2
```
**Constraints:**
- `1 <= nums.length <= 105`
- `nums[i]` is either `0` or `1`.

**Solution:**

- [x] [[Array]]

## Visual Reference

![Pattern Visual](../Assets/PATTERN_graph.svg)

## Detailed Intuition

- Model the problem as nodes and edges, then choose BFS/DFS/Topological traversal based on dependency direction.
- Track visited/state to prevent repeated processing and cycles.
- Build the answer while traversing (order, count, or connectivity result).

**Time Complexity** : O(n)<br>
**Space Complexity** : O(n)

```java
class Solution {
    public int findMaxConsecutiveOnes(int[] nums) {

        int maxCon = 0, currCon = 0;

        for (int num : nums) {
            if (num == 1) {
                currCon++;
                maxCon = Math.max(currCon, maxCon);
            } else {
                currCon = 0;
            }
        }
        return maxCon;
    }
}
```
